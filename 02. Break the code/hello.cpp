#include <iostream> // preprocessor director -> Used to include files
// iostream for taking input and printing ouput
using namespace std;
// once for import for all, otherwise we have to write std::cout << "Hello Coding !"

int main() // the execution of code begins from main function(mandatory)
{
    // used to display output [<< operator follows by cout]
    // semicolon(;) end of the line.
    cout << "Hello Coding !" << endl;

    // exit the function !
    return 0;
}
// curlibraces for open and close the function !